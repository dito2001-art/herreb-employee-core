name: Format EMP-001 Proactive Sales Branch

on:
  push:
    branches:
      - feature/emp001-proactive-sales-v0.1
    paths:
      - .github/workflows/format-emp001.yml

permissions:
  contents: write

jobs:
  format:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout EMP-001 feature branch
        uses: actions/checkout@v5
        with:
          ref: feature/emp001-proactive-sales-v0.1

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Apply canonical formatting
        run: npx oxfmt --write .

      - name: Commit formatting only if needed
        run: |
          if git diff --quiet; then
            echo "No formatting changes required."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Apply canonical EMP-001 formatting"
          git push origin HEAD:feature/emp001-proactive-sales-v0.1
